# ゴルフスコア一括登録 — 引き継ぎサマリー②（2026-07-02）

新チャットの冒頭でこのファイルを読ませてから続けてください。
前回の `HANDOVER_GOLF_SCORES.md` の続きです。J-SYSスコア画像の読み取り→検算→登録スクリプト作成、に加えて**保存層（data_manager.py）のバグ修正**まで進みました。

ユーザー（Hiro）は日本語・データ重視・直截。誤りは即修正。お世辞不要。

---

## 0. これは何の作業か
- Hiroのゴルフスコアアプリ（`C:\Users\h_minowa\golf-score-app\`、Python+Streamlit、保存先はGoogleスプレッドシート）へ、
  **J-SYS（JGAハンディキャップシステム）のスコア画面スクショ**を読み取って過去ラウンドを一括登録する。
- AIはスプレッドシートに直接書けない。成果物は**検算済みの登録用 `import_all.py`** で、Hiroがローカルで実行して書き込む。

---

## 1. ⚠️ 最優先の未解決事項（現在ここで止まっている）

**データ登録は成功済み。だがアプリがエラー表示のまま。原因は「アプリが古い `data_manager.py` をメモリに保持していること」。**

- `import_all.py` 実行 → 保存時に一度クラッシュ（下記§3の5万字上限）→ `data_manager.py` を修正して再実行 → **174ラウンドが正しくGoogleスプレッドシートに保存済み**（`diag2.py` で確認済み。データは100%無傷、全174件がdict）。
- しかし集計・分析タブで `TypeError: string indices must be integers, not 'str'`（`get_all_player_names` の `r["players"]`）が続く。
- **真因**: 実行中のStreamlitプロセスが**旧 `data_manager.py`** をメモリに保持。旧 `_load` は新形式（分割マニフェスト）を読めず、マニフェストdictをroundsとして返す→dictのキー文字列を回して落ちる。
  - 証拠: トレースバックのフレームは「line 328, in `get_all_player_names`」だが、新ファイルの328行目は `update_round` のdocstring。行番号と関数名の食い違い＝メモリ上コードとディスクの不一致。
- **解決策（Hiroに未実施）**: ブラウザ再読込やStreamlitの「Rerun」では依存モジュールは再読込されない。**プロセスごと再起動**が必要:
  1. `streamlit run app.py` を動かしているターミナルで `Ctrl+C`
  2. （必要なら）`taskkill /F /IM streamlit.exe` / `taskkill /F /IM python.exe`
  3. `streamlit run app.py` で起動し直す
- 再起動してもまだ「line 328」が出るなら、別パスの `data_manager.py` を読んでいる/二重起動、を疑い、起動時ターミナル出力全体を確認。

→ **新チャットではまず「再起動したか、174ラウンド表示されたか」を確認すること。**

---

## 2. もう一つの未解決事項：コース名の表記ゆれ（名寄せ）

- 既存DBのコース名は**正式表記**：`(社)鷹之台カンツリー倶楽部`、`三好カントリー倶楽部 東コース`、`サイアムカントリークラブ・バンコク`、`小樽カントリー倶楽部　新コース` 等（`diag2.py` で判明）。
- 今回登録した霞ヶ関は**略記**：`霞ヶ関CC 東/東` / `霞ヶ関CC 西/西`。
- import時のスキップが1件のみ（RE2内の 2023-03-24 重複だけ）だった＝**既存DBに `霞ヶ関CC 東/東` 名のラウンドは無かった**。既存に別表記の霞ヶ関があれば、集計上で**別コースに二分**される可能性大。
- **再起動後、集計・分析で霞ヶ関がどう並ぶか要確認。** 二分していたら、`霞ヶ関CC 東/東`→既存の正式表記へ**一括リネームする更新スクリプト**を出す（`courses` シートの登録名も確認して揃える）。この修正は `data_manager.py` の `update`/`load` で完結でき、既存データは壊さない。

---

## 3. 保存層バグと修正（重要な技術的背景）

**バグ**: `data_manager.py` の Google Sheets バックエンド（Hiroの環境はこれ）は、rounds全件を1つのJSON文字列にして**単一セル**へ書く設計。Googleスプレッドシートは**1セル50,000字が上限**。79→174ラウンドで超過し、`gspread APIError: more than the maximum of 50000 characters in a single cell` で保存が中断していた。

**修正（適用済みの新 `data_manager.py`、`/mnt/user-data/outputs/data_manager.py`）**:
- `_store`（gs分岐）: payloadが `_CHUNK_LIMIT=45000` を超えたら、`rounds` セルに分割マニフェスト `{"__chunked__": true, "n": N}`、本体を `rounds__0` / `rounds__1` … に文字列単純分割して格納。
- `_load`（gs分岐）: マニフェストを検出したら断片を結合して復元。**旧・単一セル形式もそのまま読める（後方互換）**。
- 書き込みは全て **`value_input_option="RAW"`**（`ws.update_cells([gspread.Cell(...)], value_input_option="RAW")` / `append_row(..., value_input_option="RAW")`）。旧コードの既定 USER_ENTERED だと、分割境界が `-`（日付）・`=`・`+`・`@` で始まったときGoogle Sheetsが式/数値と誤解釈して壊す。RAWで恒久回避。
- 変更は gs分岐のみ。pg/file バックエンド・集計関数群は無改変。`save_round` 等も `_store` 経由なので自動で分割対応。
- テスト済み: 小/大データ往復、多チャンク分割、縮小時のstaleセル空白化、旧形式の後方互換、prefs等のdict誤判定なし、RAWがコアシオンを防ぐこと。

**診断スクリプト**（読み取り専用、`/mnt/user-data/outputs/` にあり。もう使わなければ削除可）:
- `diag.py`: course_name別件数・「霞」を含む名前・代表キー存在チェック・同一日別コース重複検出。
- `diag2.py`: app_data 生セルのダンプ、rounds分割状態、結合復元の可否、load_rounds結果の型分布。

---

## 4. 登録データの状態（`import_all.py`、96レコード）

`/mnt/user-data/outputs/import_all.py`。全件検算済み（OUT+IN=Total）。実行するとGoogleスプレッドシートに書き込む（既存とは(date,course_name)で重複スキップ、冪等）。

構成: **RE1(19) + RE2(20) + RE3(20) + RE4(17) + RE5(19) + RE6(1) = 96レコード → ユニーク95**
- 重複1件: RE2内の `2023-03-24 霞ヶ関CC 東/東`（同一ラウンドの重複スクショ、自動スキップ）。
- 空DB相当で実行すると「追加95 / スキップ1」。**実際にHiroの環境で実行済み → 174ラウンドが保存済み**（既存79 + 95）。

**Hiroの指示で除外したもの（重要）**:
- `2021-02-19 霞ヶ関CC 東/東`（102）: `2021-02-22` と**18ホール全打数一致**（J-SYS二重登録疑い）。→ 除外。
- `2021-02-23 芝山GC アウト/イン`（102、千葉・倶楽部ID 12118）: Hiroが「芝山を知らない」→ 除外。付随の `SBY` パー配列も削除。

**日付範囲**: 2019-11〜2024-03の霞ヶ関中心＋他倶楽部（茨木東西/カメリア/立科/茨城/大浅間）。

---

## 5. 読み取り・登録の規則（踏襲すること）

**画像読み取り**（残りがあれば同手順）:
- J-SYS「スコアの詳細」画面、ほぼ 750×1334。
- Pillowで `im.convert("RGB").crop((185,205,725,665)).resize(×2, LANCZOS)` → 目視。プレー日・倶楽部・コース・ティー・Par行・ストローク行が収まる。
- スクロール位置が違う画像（上部に別バナー等）はクロップy位置を調整。
- **数字が合計と合わないマスは必ず再クロップ**（`crop((150,560,600,600))` 等で該当行を拡大）。今回 6→8、5→6、9→8 等の誤読を複数補正した実績あり。極端値（10、2、birdie等）も再クロップで確認。

**検算（必須）**: 各ラウンドで OUT合計 + IN合計 = カードTotal。1つでも合わなければ登録に含めない。

**レコード規則**:
- プレイヤー名は必ず `"hiroaki minowa"`。H1→H18順。
- `putts=[]`, `hdcps=None`, `yards=[]`, `num_holes=18`。
- 重複防止キー = **(date, course_name)**、日付はISO（`YYYY-MM-DD`）。
- **重複スクショ**（同一ラウンド2枚）は1件に統合。**別日付だがスコア完全一致**はJ-SYS二重登録を疑い、Hiroに判断を仰ぐ（勝手にどちらか消さない）。

**コース名の付け方（要注意・§2参照）**: 現状の `import_all.py` は略記（`霞ヶ関CC 東/東`）。だが**既存DBは正式表記**。新規ラウンドを足す前に、既存DBの表記に合わせるか決めること。

---

## 6. パー配列（確定済み・SBYは削除済み）

```python
KW   = [4,4,5,3,4,5,3,4,4, 5,3,4,5,4,5,3,4,4]  # 霞ヶ関CC 西/西  Par73
KE   = [4,4,4,3,5,4,3,5,4, 3,4,4,4,5,4,3,4,4]  # 霞ヶ関CC 東/東  Par71
KEIN = [3,4,4,4,5,4,3,4,4, 3,4,4,4,5,4,3,4,4]  # 霞ヶ関CC 東IN/東IN(連結) Par70
CAM  = [5,4,4,3,4,4,4,5,3, 4,4,4,3,5,3,4,4,5]  # カメリアヒルズCC アウト/イン Par72
IBK  = [4,5,3,4,3,4,5,4,4, 4,4,4,5,3,4,4,3,5]  # 茨木CC 西/西  Par72
IBKE = [4,5,4,3,5,3,4,4,4, 3,4,4,5,4,3,5,4,4]  # 茨木CC 東/東  Par72（今回追加）
TAT  = [4,5,3,4,4,4,3,4,5, 4,3,4,4,3,5,4,4,5]  # 立科GC アウト/イン Par72
IBG  = [5,3,4,4,4,3,4,4,5, 4,4,4,3,4,5,4,3,5]  # 茨城GC 東/東  Par72
OAS  = [4,3,4,4,5,4,3,5,4, 5,4,3,4,4,5,3,4,4]  # 大浅間GC アウト/イン Par72
# 新コースが出たら、その画面のPar行(H1-18)をそのまま新配列にする。
```

---

## 7. 成果物ファイル（Hiroのローカルにある/出力済み）
- `import_all.py` … 96レコードの登録スクリプト（実行済み・174件登録済み）。
- `data_manager.py` … チャンク分割＋RAW書き込みのパッチ版。**アプリ再起動で有効化が必要**（§1）。
- `diag.py` / `diag2.py` … 診断用（不要なら削除可）。

---

## 8. 次にやること（優先順）
1. **アプリを完全再起動**して 174ラウンド表示を確認（§1）。まだRAW版を適用していなければ先に差し替え。
2. 集計・分析で**霞ヶ関の名寄せ**を確認。二分していたらリネーム更新スクリプトを作る（§2）。
3. 残りのJ-SYS画像があれば同手順で読み取り→検算→`import_all.py` に追記して再実行（(date,course)で自動スキップ）。※画像は「100枚中94枚が前チャットにあった」状態。残り数枚の有無をHiroに確認。

---

## 9. 新チャットでの最初の一言（例）
「このHANDOVERを読んでゴルフスコア作業を引き継いで。まず、アプリを再起動して174ラウンドが表示されたか確認したい。
その後、霞ヶ関のコース名が既存の正式表記と二分していないか見て、必要なら名寄せスクリプトを作って。
残りのJ-SYS画像があれば同じ手順（クロップ拡大→OUT+IN=Total検算→(date,course)で重複防止）で読み取って import_all.py に追記して。」
